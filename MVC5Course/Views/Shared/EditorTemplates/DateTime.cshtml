@model DateTime?

@if (Model.HasValue)
{
    @Html.TextBox("", Model.Value.ToString("yyyy-MM-dd"), new { type = "date", @class = "form-control" })
}
else
{
    @Html.TextBox("", null, new { type = "date", @class = "form-control" })
}